export function paraMaiusculas(texto) {
    return `${texto} em maiúsculo: ${texto.toUpperCase()}`
}

export function paraMinusculas(texto) {
    return `${texto} em minúsculo: ${texto.toLowerCase()}`
}